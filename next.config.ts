import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      'picsum.photos',
      'via.placeholder.com',
      'placekitten.com',
      'source.unsplash.com',
      'dummyimage.com'
    ]
  }
};

export default nextConfig;
